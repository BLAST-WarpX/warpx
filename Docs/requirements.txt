# Copyright 2019 Axel Huebl, Maxence Thevenet
#
# This file is part of WarpX.
#
# License: BSD-3-Clause-LBNL

# WarpX PICMI bindings w/o C++ component (used for autoclass docs)
-e ../Python
# render the pydantic-based PICMI classes (and warpx_ extensions) in the API docs:
autodoc_pydantic>=2
breathe
docutils>=0.17.1

# PICMI API docs
# note: keep in sync with version in ../requirements.txt
# This development branch documents the in-progress PICMI pydantic refactoring
# (picmi-standard/picmi#133). Restore the released pin below once it is merged & released:
# picmistandard==0.34.0
picmistandard @ git+https://github.com/chillenzer/picmi.git@pydantic-refactoring

pybtex
pygments
recommonmark
# Sphinx<7.2 because we are waiting for
#   https://github.com/breathe-doc/breathe/issues/943
sphinx>=5.3,<7.2
sphinx-copybutton
sphinx-design
sphinx_rtd_theme>=1.1.1
sphinxcontrib-bibtex
sphinxcontrib-googleanalytics
sphinxcontrib-napoleon
yt  # for checksumAPI
