# base input parameters
FILE = inputs_test_3d_electrostatic_sphere_lab_frame

# test input parameters
# Use several boxes per MPI rank so that the coarse multigrid levels would be
# agglomerated onto a single box by default, then switch that off to exercise
# the distributed coarse levels.
amr.max_grid_size = 32
warpx.self_fields_agglomeration = 0
warpx.self_fields_consolidation = 0
